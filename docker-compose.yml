services:
  mysql:
    image: mysql:8
    container_name: mysql-club
    restart: unless-stopped
    environment:
      MYSQL_ROOT_PASSWORD: ${DB_PASSWORD:-root}
      MYSQL_DATABASE: ${DB_NAME:-Club_Deportivo}
    ports:
      - "${DB_PORT:-3306}:3306"
    volumes:
      - ./db/init_db.sql:/docker-entrypoint-initdb.d/init_db.sql:ro
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data:
